#arquivo para ci.cd AULA TI323 2026
name: CI/CD TI323

on: [push] # como será o gatilho

jobs: #trabalho a ser realizado
  informação_sistema:
    runs-on: ubuntu-latest
    steps: #passos a serem executador
      - name: Listar o diretorio corrente
        run: ls -la